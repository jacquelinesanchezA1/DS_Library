{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "C++ Launch (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/main",  // Points to the compiled program
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "console": "integratedTerminal",
      "preLaunchTask": "build"
    }
  ]
}
